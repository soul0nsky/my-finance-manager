# 💰 My Finance Manager

**Система управления личными финансами** с учётом доходов, расходов и бюджетированием по категориям.

Терминальное приложение для контроля личных финансов с поддержкой нескольких пользователей.

**Итоговый проект** по дисциплине "Принципы ООП" (МИФИ).

---

## 📋 Описание

Сервис позволяет:
- Регистрировать пользователей с персональными кошельками
- Добавлять доходы и расходы по категориям
- Устанавливать бюджеты на категории расходов
- Получать статистику по финансам (общую и по категориям)
- Получать оповещения при превышении бюджета
- Переводить средства между пользователями
- Экспортировать и импортировать данные в CSV

Каждый пользователь идентифицируется по логину и паролю. Данные сохраняются в JSON-файл.

---

## 🏗 Архитектура

Проект построен по принципам **Clean Architecture** с чётким разделением слоёв:

```
ru.mifi.financemanager/
├── domain/           # Доменные модели (User, Wallet, Transaction)
├── repository/       # Слой данных (интерфейс + JSON-реализация)
├── service/          # Бизнес-логика (Auth, Finance, Notification)
├── config/           # Конфигурация из файла
├── export/           # Экспорт/импорт CSV
├── exception/        # Пользовательские исключения
└── cli/              # Консольный интерфейс
```

**Основные паттерны:**
- Repository Pattern для абстракции хранилища
- Dependency Injection через конструкторы
- Strategy Pattern в экспорте данных
- BigDecimal для точных финансовых расчётов

---

## 🚀 Быстрый старт

### Требования
- Java 21+
- Maven 3.6+

### Сборка и запуск

```bash
# Клонировать репозиторий
git clone https://github.com/your-username/finance-manager.git
cd finance-manager

# Собрать проект
mvn clean package

# Запустить приложение
java -jar target/finance-manager-1.0.0-jar-with-dependencies.jar
```

### Альтернативные команды

```bash
# Быстрый запуск через Maven (для разработки)
mvn compile exec:java

# С очисткой кэша (если были изменения в конфигурации)
mvn clean compile exec:java

# Запуск тестов
mvn test

# Проверка стиля кода
mvn spotless:check
mvn spotless:apply
```
---
## ⚙️ Конфигурация

### Настройка проекта

Перед первым запуском **скопируйте** файл конфигурации и укажите свои значения:
```bash
# Скопируйте пример конфигурации
cp config.properties.example config.properties

# Отредактируйте файл со своими настройками
nano config.properties
```

### Параметры конфигурации

Настройки можно изменить в файле `config.properties`:

```properties
# Path to data file
app.data.file=data/users.json

# Budget warning threshold in percentage (notification at 80%)
app.budget.warning.threshold=80.0

# Default currency
app.currency=RUB
```
* Примечание: Если файл `config.properties` не существует, приложение использует значения по умолчанию из `src/main/resources/application.properties`.
---

## 📖 Как пользоваться

### Первый запуск

При первом запуске выберите "Регистрация" — создайте логин и пароль:

```
╔════════════════════════════════════════════════════════════╗
║       СИСТЕМА УПРАВЛЕНИЯ ЛИЧНЫМИ ФИНАНСАМИ                 ║
║                     версия 1.0.0                           ║
╚════════════════════════════════════════════════════════════╝

=== МЕНЮ АВТОРИЗАЦИИ ===
1. Вход в систему
2. Регистрация
3. Список пользователей
0. Выход

Выберите действие: 2
Придумайте логин: ivan
Придумайте пароль: password123
✅ Пользователь ivan успешно зарегистрирован!

Выберите действие: 1
Логин: ivan
Пароль: password123
✅ Добро пожаловать, ivan!
   Текущий баланс: 0,00 ₽
```

### Основные команды

| Команда | Описание |
|---------|----------|
| `1` | Добавить доход |
| `2` | Добавить расход |
| `3` | История операций |
| `4` | Установить бюджет на категорию |
| `5` | Просмотреть бюджеты |
| `6` | Общая статистика |
| `7` | Статистика по категориям |
| `8` | Статистика за период |
| `9` | Перевод другому пользователю |
| `10` | Экспорт в CSV |
| `11` | Импорт из CSV |
| `12` | Справка (help) |
| `0` | Выход из аккаунта |

### 📝 Детальные сценарии использования

**Полные пошаговые инструкции** по всем функциям проекта доступны в документе:

👉 **[docs/use_cases.md](docs/use_cases.md)** — Сценарии ручного тестирования

Там вы найдете:
- 14 подробных сценариев тестирования всех функций
- Примеры диалогов с системой
- Ожидаемые результаты по ТЗ
- Проверку всех требований технического задания
- Команды для быстрого запуска

---

## 💾 Формат хранения данных

Данные сохраняются в JSON формате (`data/users.json`):

```json
[
  {
    "login": "ivan",
    "password": "password123",
    "wallet": {
      "transactions": [
        {
          "id": "a1b2c3d4",
          "type": "INCOME",
          "amount": 50000,
          "category": "Зарплата",
          "description": "За январь",
          "createdAt": "2025-01-11T10:30:00"
        }
      ],
      "categoryBudgets": {
        "Еда": 15000
      }
    }
  }
]
```

---

## 🧪 Тестирование

Проект содержит **72 автоматических теста** с покрытием основных сценариев:

### Unit-тесты
- `WalletTest` — транзакции, баланс, бюджеты, фильтрация (20 тестов)
- `TransactionTest` — создание, типы, equals/hashCode (8 тестов)
- `AuthServiceTest` — регистрация, вход, сессии (25 тестов)
- `FinanceServiceTest` — операции, бюджеты, статистика (18 тестов)
- `InputValidatorTest` — валидация всех типов ввода (9 тестов)

### Интеграционные тесты
- `FinanceIntegrationTest` — полные сценарии из ТЗ (8 тестов):
  - Регистрация → вход → операции → выход
  - Установка бюджета → траты → превышение
  - Перевод между пользователями
  - Несколько пользователей с изолированными данными
  - Сохранение и загрузка данных между сессиями

### Запуск тестов

```bash
# Все тесты
mvn test

# Только unit-тесты
mvn test -Dtest=**/*Test

# Только интеграционные тесты
mvn test -Dtest=**/*IntegrationTest
```

---

## 📁 Структура проекта

```
finance-manager/
├── pom.xml                                    # Maven конфигурация
├── README.md                                  # Документация
├── config.properties.example                  # Пример конфигурации (отредактируйте)
├── docs/
│   └── use_cases.md                           # Сценарии ручного тестирования
├── .github/
│   └── workflows/
│       └── ci.yml                             # GitHub Actions CI
└── src/
    ├── main/
    │   ├── java/ru/mifi/financemanager/
    │   │   ├── FinanceApplication.java        # Entry point
    │   │   ├── domain/
    │   │   │   ├── User.java                  # Модель пользователя
    │   │   │   ├── Wallet.java                # Кошелёк с операциями и бюджетами
    │   │   │   ├── Transaction.java           # Финансовая операция
    │   │   │   └── TransactionType.java       # enum INCOME / EXPENSE
    │   │   ├── repository/
    │   │   │   ├── UserRepository.java        # Интерфейс репозитория
    │   │   │   ├── JsonUserRepository.java    # JSON-реализация с Gson
    │   │   │   └── LocalDateTimeAdapter.java  # Gson адаптер для дат
    │   │   ├── service/
    │   │   │   ├── AuthService.java           # Интерфейс аутентификации
    │   │   │   ├── AuthServiceImpl.java       # Регистрация, вход, logout
    │   │   │   ├── FinanceService.java        # Интерфейс финансовых операций
    │   │   │   ├── FinanceServiceImpl.java    # Доходы, расходы, бюджеты, переводы
    │   │   │   └── NotificationService.java   # Оповещения о бюджете
    │   │   ├── config/
    │   │   │   └── AppConfig.java             # Загрузка конфигурации
    │   │   ├── cli/
    │   │   │   ├── ConsoleApp.java            # Главный CLI с меню
    │   │   │   └── InputValidator.java        # Валидация пользовательского ввода
    │   │   ├── export/
    │   │   │   ├── DataExporter.java          # Интерфейс экспорта
    │   │   │   ├── CsvExporter.java           # Экспорт в CSV
    │   │   │   └── CsvImporter.java           # Импорт из CSV
    │   │   └── exception/
    │   │       ├── ValidationException.java   # Ошибки валидации
    │   │       └── InvalidCredentialsException.java # Неверные учётные данные
    │   └── resources/
    │       └── application.properties         # Конфигурация по умолчанию
    └── test/
        └── java/ru/mifi/financemanager/
            ├── domain/
            │   ├── WalletTest.java            # Тесты кошелька
            │   └── TransactionTest.java       # Тесты транзакций
            ├── service/
            │   ├── AuthServiceTest.java       # Тесты аутентификации
            │   └── FinanceServiceTest.java    # Тесты финансовых операций
            ├── cli/
            │   └── InputValidatorTest.java    # Тесты валидации ввода
            └── integration/
                └── FinanceIntegrationTest.java # Интеграционные тесты
```

---

## ✅ Соответствие ТЗ

### Базовый уровень (30 баллов)

| Критерий | Баллы | Реализация |
|----------|-------|------------|
| Авторизация пользователей | 2 | `AuthService` + `UserRepository`, вход по логину/паролю, поддержка нескольких пользователей |
| CLI-взаимодействие и цикл команд | 2 | `ConsoleApp` с while-loop, числовое меню, корректный выход с сохранением данных |
| Доходы и расходы | 4 | `Transaction` + `FinanceService.addIncome/addExpense()`, BigDecimal для точных расчётов |
| Кошелёк пользователя | 3 | `Wallet` привязан к `User`, хранит список транзакций и бюджеты по категориям |
| Категории и бюджеты | 5 | `Wallet.setBudget()`, расчёт остатка через `getRemainingBudget()`, учёт в статистике |
| Вывод информации и статистика | 4 | `FinanceService.getExpensesByCategory()`, сводки по категориям, остаток бюджета |
| Оповещения | 3 | `NotificationService` — уведомления при превышении бюджета и перерасходе |
| Сохранение и загрузка данных | 3 | `JsonUserRepository.flush()/load()`, JSON через Gson, сохранение при выходе |
| Валидация ввода | 2 | `InputValidator` — суммы, категории, логины, подтверждения с понятными сообщениями |
| Разделение по классам | 2 | domain → repository → service → cli, интерфейсы для DI, SOLID принципы |

### Средний уровень (10 баллов)

| Критерий | Баллы | Реализация |
|----------|-------|------------|
| Подсчёты по выборкам | 2 | `getTransactionsByPeriod()`, `getExpenseByCategories()`, уведомление если категория не найдена |
| Редактирование бюджетов | 2 | `setBudget()` / `removeBudget()` с валидацией, изменение существующих лимитов |
| Экспорт/импорт CSV | 2 | `CsvExporter` + `CsvImporter` с обработкой ошибок и статистикой |
| Расширенные оповещения | 2 | Предупреждение при 80% лимита, уведомление о перерасходе, отрицательный баланс |
| UX CLI | 2 | Команда help, примеры использования, форматированные таблицы, понятные ошибки |

### Высокий уровень (10 баллов)

| Критерий | Баллы | Реализация |
|----------|-------|------------|
| Тестирование | 3 | 72 теста: unit (Wallet, Transaction, Auth, Finance, Validator) + интеграционные сценарии |
| Архитектура и качество кода | 3 | Clean Architecture, интерфейсы репозиториев/сервисов, DI через конструкторы |
| Автоматизация и сборка | 2 | Maven + Spotless (Google Java Format) + GitHub Actions CI |
| Документация | 2 | README с инструкцией, примерами команд, описанием архитектуры, детальными сценариями |

### Дополнительное задание

| Критерий | Реализация |
|----------|------------|
| Переводы между пользователями | `FinanceService.transfer()` — расход у отправителя, доход у получателя, идентификация по логину, проверка баланса |

**Итого: 50/50 баллов + бонус**

---

## 🔧 Технологии

- **Java 21** — современные возможности языка
- **Maven** — управление зависимостями и сборка
- **Gson** — сериализация/десериализация JSON
- **JUnit 5** — тестирование
- **Spotless** — автоматическое форматирование кода (Google Java Format)
- **GitHub Actions** — CI/CD

---

## 📝 Лицензия

MIT License. Проект создан в учебных целях.

---

## 👤 Автор

**Солонский Михаил**  
Студент МИФИ, 2025


# Application metadata
app.name=Finance Manager
app.version=1.0.0

# Path to data file (can be overridden by config.properties)
app.data.file=data/users.json

# Budget warning threshold in percentage (notification at 80%)
app.budget.warning.threshold=80.0

# Default currency
app.currency=RUB